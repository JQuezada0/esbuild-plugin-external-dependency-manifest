{
  "type": "object",
  "properties": {
    "packagePath": {
      "type": "string",
      "description": "The path of the package to publish"
    },
    "packageName": {
      "type": "string",
      "description": "The name to use for the package"
    }
  }
}